# Vyper version 0.3.0
struct Deposits:
    amount: uint256
    timestamp: uint256

deposits: public(HashMap[address, Deposits])




def withdraw(contract_address: address, amount: uint256):
    assert contract_address.is_contract, "Contract is not a contract"
    assert contract_address != empty(address), "Address cannot be zero"
    

# So an user will have multiple deposits between de pool so we need 

